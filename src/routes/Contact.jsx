import { MetaTags } from 'react-meta-tags';
import Layout from '../components/Layout';

export default function Contact() {
  return (
    <Layout noNav>
      <MetaTags>
        <title>Contact Me | Michael Peter</title>
      </MetaTags>
    </Layout>
  );
}
